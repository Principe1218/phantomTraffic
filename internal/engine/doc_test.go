package engine

import "testing"

// TestPackageCompiles is a placeholder guard asserting the engine package
// exists and compiles. It is the first test in the package; until doc.go
// declares `package engine`, the package does not exist and this fails to build.
func TestPackageCompiles(t *testing.T) {
	t.Parallel()
}
